name: Lint
on:
  push:
    branches:
      - master
jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Shellcheck
        run: config ls-files | grep -E '(.bash_profile|\.sh)' | xargs shellcheck
